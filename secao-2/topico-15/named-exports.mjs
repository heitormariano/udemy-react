const myCube = (number) => {
  return number * number * number;
};

const myObj = {
  firstName: 'Heitor',
  lastName: 'Camara',
};

const value = Math.PI + Math.sqrt(4);

export { myCube, myObj, value };
